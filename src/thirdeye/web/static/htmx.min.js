/* htmx v2.0.4 - vendored 2026-05-25 - https://htmx.org */
/* PLACEHOLDER: real htmx 2.0.4 bytes could not be fetched in the sandboxed
   build environment. Replace this file by running:
       curl -fsSL https://unpkg.com/htmx.org@2.0.4/dist/htmx.min.js \
         -o src/thirdeye/web/static/htmx.min.js
   then re-prepend the version header line above. The library defines
   canonical attributes such as hx-get, hx-post, hx-target, hx-swap,
   hx-trigger which feature templates rely on. */
(function () {
  if (typeof window !== 'undefined' && !window.htmx) {
    window.htmx = {
      version: '2.0.4-placeholder',
      on: function () {},
      off: function () {},
      trigger: function () {},
      ajax: function () {},
      find: function () {},
      findAll: function () { return []; },
      process: function () {}
    };
    console.warn('htmx placeholder loaded; install real htmx 2.0.4 to enable hx-get/hx-post.');
  }
})();
